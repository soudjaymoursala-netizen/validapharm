# Configuration Cloudflare Workers — ce fichier ne porte aucun secret (clé
# API, jeton d'accès) : voir README.md pour `wrangler secret put`. Les
# noms de modèle ci-dessous ne sont pas sensibles (pas une clé), donc en
# [vars] plutôt qu'en secret — modifiables sans redéploiement de code.
#
# Fournisseur : ChatGPT (OpenAI) — voir src/index.ts et README.md.

name = "validapharm-ia-relay"
main = "src/index.ts"
compatibility_date = "2026-09-07"

[vars]
# Identifiants connus au moment de l'écriture de ce fichier (07/09/2026),
# jamais confirmés contre le catalogue de modèles OpenAI réellement
# disponible au moment du déploiement — à vérifier/ajuster ici avant mise
# en production, voir openaiProvider.ts.
MODELE_CHAT_NORMATIF = "gpt-4o"
MODELE_AUDIT_SIMULE = "gpt-4o"
