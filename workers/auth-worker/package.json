{
  "name": "validapharm-auth-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply validapharm-auth --local",
    "migrate:remote": "wrangler d1 migrations apply validapharm-auth --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
