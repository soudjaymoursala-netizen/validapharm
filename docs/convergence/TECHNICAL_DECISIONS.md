# TECHNICAL_DECISIONS — Registre des décisions techniques structurantes

| | |
|---|---|
| **Statut** | 6ᵉ livrable de la Phase 0. Chaque entrée précise si elle est **actée** (recommandation claire, pas d'arbitrage réel requis — le master prompt §"Quand demander une décision" dit d'avancer dans ce cas) ou **proposée** (arbitrage réel nécessaire, décision de l'utilisateur requise avant mise en œuvre). |

---

### TD-001 — Étendre le pattern serverless existant (Cloudflare Workers) plutôt qu'un backend "Modular Monolith"
- **Statut** : **ACTÉE (25/08/2026, décision explicite de l'utilisateur)** — résout CONFLICT-001. Version révisée et simplifiée par rapport à la première proposition (backend serveur générique), sur demande explicite de l'utilisateur de challenger la solution la plus simple ne compromettant pas l'objectif.
- **Context** : PWA 100% navigateur actuelle, contrainte réelle du poste professionnel (pas d'installation locale). La Target Architecture présuppose un backend (Modular Monolith, base relationnelle, recherche, workers asynchrones) pour porter Source Intelligence/Search/Integration Gateway.
- **Problem** : la première option envisagée (introduire un vrai backend serveur — base relationnelle + stockage objet + moteur de recherche) est plus lourde que ce que l'objectif exige réellement à l'échelle actuelle du projet (mono-utilisateur), et ouvre une nouvelle surface d'infrastructure à héberger/sécuriser/sauvegarder.
- **Decision** : ne pas construire de backend serveur "Modular Monolith". À la place, **étendre le pattern serverless déjà construit et validé** (relais Cloudflare Workers, URS-NF-044ter, déjà testé joignable depuis le poste professionnel de l'utilisateur — AR-R-64, clos) :
  - **OCR/Document Intelligence** : un second Worker sans état, relais vers une API cloud de vision/OCR (même pattern exact que le relais IA existant) — pas de pipeline serveur dédié.
  - **Recherche** : index plein texte calculé côté navigateur (IndexedDB) pour l'usage actuel ; si le volume l'impose un jour, les embeddings deviennent de simples fichiers JSON dans le dépôt Git (déjà source de vérité), comparés côté client — pas de base vectorielle serveur.
  - **Stockage des sources/preuves** : le dépôt Git dédié existant, pas un nouvel object storage.
  - **Traitement asynchrone** : Cloudflare Queues (même compte Cloudflare que le relais IA), suffisant à l'échelle mono-utilisateur actuelle — pas d'orchestrateur de jobs serveur.
- **Pros** : zéro serveur qui tourne en continu, zéro nouvelle base de données à héberger/sécuriser/sauvegarder, zéro nouvelle dépendance de compte (même Cloudflare, même GitHub déjà utilisés et validés), risque et coût minimaux.
- **Cons** : pas de moteur de requêtes relationnelles complexes multi-entités à grande échelle — accepté explicitement car aucun besoin réel ne le démontre aujourd'hui (cohérent avec le principe de la cible : "un service n'est extrait que si un besoin démontré existe").
- **Risk** : Faible — réutilise une brique déjà en production et déjà testée réseau.
- **Rejected Alternatives** : (a) rester 100% navigateur sans aucune extension serverless — rejeté, dégraderait trop l'ambition Document Intelligence/Search ; (b) backend serveur complet (Modular Monolith, base relationnelle, object storage dédié) — rejeté comme disproportionné à l'échelle actuelle du projet, réévaluable seulement si un besoin réel et démontré apparaît.
- **Impact** : Débloque les Phases 7-8 du `CONVERGENCE_PLAN.md` sans point d'arrêt.
- **Reversibility** : Élevée — une fonction serverless ponctuelle se remplace ou s'étend sans migration lourde ; si un vrai besoin de backend complet apparaît plus tard, rien de ce qui est construit ici ne devient un frein (le Git reste la source de vérité dans tous les cas).
- **Complément (25/08/2026, Phase 6, décision explicite de l'utilisateur)** : le Worker OCR est réellement construit (`workers/ocr-relay/`). Fournisseur retenu : **Azure AI Vision (Read API)** — meilleur niveau gratuit au moment de la décision (5000 transactions/mois contre 1000 chez Google Cloud Vision, vérifié par recherche le jour même), déjà approuvé par l'utilisateur au même titre que Google Cloud Vision. Architecture délibérément swappable (`FournisseurOcr` interface, un seul fichier à ajouter/changer pour basculer vers Google Cloud Vision). Déploiement réel (compte Cloudflare/Azure, secrets, test de joignabilité réseau équivalent à AR-R-64) **reste à faire par l'utilisateur** — non exécutable depuis cette session distante, voir `workers/ocr-relay/README.md`.

---

### TD-002 — Remplacer la grille ACFC codée en dur par un `MethodProfile` configurable
- **Statut** : **ACTÉE** (recommandation claire, aucun arbitrage réel — convergence de 3 sources indépendantes : repo, mémoire de session, Target Architecture).
- **Context** : `grilleCriticite.ts` code en dur 9 critères, viole une interdiction explicite du package (*"Ne pas hard-coder 6/7/9 questions ACFC"*).
- **Problem** : impossible d'accueillir un client réel avec ses propres questions (4 à 10 selon les entreprises réelles étudiées).
- **Options** : garder tel quel (rejeté) vs `MethodProfile` (questions mot pour mot, version, source, date d'effet, règle de décision configurable).
- **Pros/Cons** : `MethodProfile` demande une réécriture du module et de ses 11 tests, mais aucune autre partie du code n'en dépend (remplacement isolé, faible risque).
- **Risk** : Faible techniquement, élevé si non traité (violation documentée qui persiste).
- **Recommendation** : à implémenter en priorité, cf. `GAP.md`.
- **Rejected Alternatives** : garder la grille figée avec un simple "mode client" en paramètre — rejeté, ne résout pas le problème de fond (les questions elles-mêmes doivent être données, pas choisies parmi des jeux prédéfinis).
- **Impact** : Élevé et positif (débloque tout le futur module Assessment).
- **Reversibility** : Élevée — c'est un module isolé, changement sans effet de bord.

---

### TD-003 — Corriger l'URS v25 (fusion ACFC/CSA, modèle d'impact à 3 niveaux, référence au gabarit CSV inexistant)
- **Statut** : **ACTÉE**.
- **Context** : CONFLICT-002 et CONFLICT-003.
- **Problem** : l'URS actuelle contredit à la fois la Target Architecture et les normes réelles déjà étudiées (Ferring FSMP, ISPE Baseline Guide).
- **Options** : garder l'URS telle quelle (rejeté, contredit 3 sources) vs corriger (restructurer §10 famille F en 3 briques distinctes : Impact Assessment, Computer System Assessment, Risk Analysis/ACFC).
- **Recommendation** : corriger avant toute construction du futur module d'assessment.
- **Impact** : Documentaire, mais bloquant pour construire juste du premier coup.
- **Reversibility** : Élevée (changement de texte, pas de code).

---

### TD-004 — Séquencer le Source/Document Intelligence en 2 temps (structuration manuelle assistée d'abord, compréhension P&ID/schémas complexes ensuite)
- **Statut** : **ACTÉE** (recommandation de séquencement, pas d'arbitrage de fond).
- **Context** : CHALLENGE-001.
- **Recommendation** : phase 1 = OCR + structuration assistée avec validation humaine systématique ; phase 2 = compréhension de schémas techniques complexes, seulement après retour d'expérience réel.
- **Impact** : Réduit le risque de fausse confiance sur des extractions de schémas critiques (alarmes, interlocks).
- **Reversibility** : Élevée (c'est un ordre d'implémentation, pas une architecture figée).

---

### TD-005 — Extraire une interface `Connector` générique à partir de `GitHubConnector`/`DriveConnector`
- **Statut** : **ACTÉE**.
- **Context** : les deux connecteurs existants sont solides et testés mais chacun a sa propre forme d'erreurs, pas d'abstraction commune.
- **Recommendation** : ADAPT — extraire l'interface commune sans réécrire la logique interne déjà validée (contre `fetch` mocké + vérifications réelles en navigateur).
- **Impact** : Prépare l'arrivée future de connecteurs QMS tiers (Veeva/SAP, backlog #32) sans dette supplémentaire.
- **Reversibility** : Élevée.

---

### TD-006 — Migrer `Client` (plat) vers un scope hiérarchique `Organization/Workspace/Site`, en dernier dans l'ordre de convergence
- **Statut** : **ACTÉE sur le principe, mécanisme précis à détailler dans `CONVERGENCE_PLAN.md`.**
- **Context** : `client_id` est utilisé par la quasi-totalité des stores actuels — c'est la clé d'isolation la plus répandue du repository.
- **Problem** : migration à haut risque de régression si faite tôt ou en un seul commit.
- **Recommendation** : traiter en dernier dans l'ordre de convergence, `Client` devenant un cas particulier à un seul niveau d'`Organization` plutôt qu'un remplacement complet — migration progressive, jamais en un seul commit (cohérent avec l'interdiction explicite du package : *"Pas de Big Bang"*).
- **Impact** : Majeur si mal fait, mais différable sans bloquer le reste (aucun autre chantier prioritaire n'en dépend).
- **Reversibility** : Faible une fois engagée — raison pour la placer en dernier, quand tout le reste du modèle est stabilisé.

---

### TD-007 — Le moteur de raisonnement (Mission→Context→AI) s'orchestre côté navigateur, jamais un nouveau backend
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `PHASE_13_17_REVUE_PANEL_MOTEUR_RAISONNEMENT.md` Question A).
- **Context** : la vision utilisateur (raisonnement multi-étapes : changement→impact→risque→requirement→test manquant, avec questions ciblées) demande un raisonnement outillé sur les stores du domaine, pas un simple prompt.
- **Problem** : un backend serveur avec état violerait TD-001 (extension serverless plutôt que backend complet, déjà actée) sans fait nouveau le justifiant.
- **Recommendation** : la boucle d'orchestration (appeler l'IA, lire son besoin d'information, interroger les stores Dexie/Pinia existants comme des "outils", redonner la réponse, recommencer) s'exécute entièrement dans le navigateur ; le relais Cloudflare reste un simple proxy sans état masquant la clé (URS-NF-044ter, inchangé). Conditions posées par E1/E4 : `AIRequest` trace chaque appel d'outil (jamais seulement le prompt/réponse final) ; la configuration du moteur (prompts, outils, version de modèle) est versionnée (`AIConfiguration`/`AIModelVersion`) pour rester reconstructible a posteriori.
- **Impact** : Structurant pour la Phase 15 — un seul nouveau module (`logique-metier/raisonnement/`), aucune nouvelle infrastructure d'hébergement.
- **Reversibility** : Élevée — pas de backend introduit, donc pas de dette d'hébergement à défaire si le choix est révisé plus tard.

---

### TD-008 — Les propositions de l'IA portent un état de confiance discret (5 états), jamais un score numérique
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `PHASE_13_17_REVUE_PANEL_MOTEUR_RAISONNEMENT.md` Question B).
- **Context** : le moteur de raisonnement doit pouvoir distinguer ce qu'il sait, ce qu'il déduit, ce qu'il ignore et ce qui est en conflit entre sources — jamais une seule couleur de confiance globale.
- **Problem** : un score numérique (0-100 %) inviterait mécaniquement à fixer un seuil d'acceptation automatique — même erreur déjà interdite pour la promotion `Parameter`→`CPP` (§10 `01_ARCHITECTURE_MASTER_FINAL.md`) et contraire au principe fondateur n°1 (l'IA n'est jamais seule source de vérité).
- **Recommendation** : type discriminant fermé `connu | inféré | inconnu | conflit | a_verifier`, même pattern que `StatutKnowledgeItem`/`StatutTestCandidate` déjà en place.
- **Impact** : Structurant pour le type `AIResponse` (Phase 15) — condition non négociable, pas une option de configuration.
- **Reversibility** : Élevée à ce stade (rien n'est encore codé) ; deviendrait faible une fois des données réelles créées avec ce schéma.

---

### TD-009 — Domaine "Work" limité à `Mission`/`Activity` pour ce lot ; `WorkflowDefinition`/`WorkflowInstance`/`Approval` différés
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `PHASE_13_17_REVUE_PANEL_MOTEUR_RAISONNEMENT.md` Question C).
- **Context** : le modèle cible nomme cinq entités dans le domaine "Work" (`Mission, Activity, Dependency, WorkflowDefinition, WorkflowInstance, WorkflowStep, Approval`).
- **Problem** : un workflow d'approbation générique construit sans cas réel pour le calibrer risquerait de fabriquer une mécanique ne correspondant à aucun processus qualité documenté (règle "ne jamais fabriquer de contenu réglementaire") — et un mécanisme d'approbation implicite existe déjà ailleurs (`QualityEvent`, `Confirmation`) tant qu'un besoin *générique transverse* n'est pas démontré.
- **Recommendation** : construire `Mission`/`Activity` (Phase 13) — suffisant pour que le moteur de raisonnement (TD-007) ait un objet à référencer. `WorkflowDefinition`/`WorkflowInstance`/`Approval` restent nommés dans le modèle cible mais non engagés, même statut documentaire que `8b`/`9-Generate-Render-Approve-Freeze` déjà différés dans `CONVERGENCE_PLAN.md`.
- **Impact** : Réduit le périmètre de la Phase 13 sans fermer la porte à un futur incrément dédié.
- **Reversibility** : Élevée — purement additif si un besoin réel de workflow générique apparaît plus tard.

---

### TD-010 — "Validation State" (état de validation vivant) : une vue calculée diagnostique, jamais un remplacement automatique de `qualification_status`
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `REVUE_PANEL_VISION_VALIDATION_ENGINEERING.md` Point A, en réponse au document de vision "Validation Engineering Platform" de l'utilisateur).
- **Context** : la vision demande de savoir à tout moment "si un système est toujours dans un état validé et pourquoi", recalculé après un changement détecté (VALIDATED → CHANGE DETECTED → IMPACT ANALYSIS → REQUALIFICATION REQUIRED → ... → VALIDATED).
- **Problem** : `AssetNode.qualification_status` (Structure Système) est aujourd'hui un statut saisi manuellement. Le dériver *automatiquement* d'une analyse d'impact reviendrait à faire prendre une décision réglementaire par une simple logique déterministe sans confirmation humaine — même erreur déjà interdite pour `CriticalParameter → CPP` (invariant #8, principe fondateur n°1).
- **Recommendation** : "Validation State" est une **fonction de lecture pure/diagnostique** (candidat naturel : un nouvel outil du Reasoning Engine, Phase 15) qui répond "voici ce que je constate et pourquoi, avec citations vers le graphe" — jamais une écriture automatique de `qualification_status`. Le statut officiel reste un acte humain distinct, déjà tracé (même patron que `Confirmation`/`TestCandidate`).
- **Impact** : Oriente une future extension du Reasoning Engine (au-delà de Phase 15) — n'introduit aucune nouvelle entité stockée dans l'immédiat.
- **Reversibility** : Élevée — fonction de lecture pure, aucune migration de schéma engagée par cette décision elle-même.

---

### TD-011 — GxP-by-design (e-signature/RBAC) : limite assumée documentée, TD-001 non rouverte, aucun garde-fou de façade
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `REVUE_PANEL_VISION_VALIDATION_ENGINEERING.md` Point B).
- **Context** : la vision liste electronic signatures/RBAC/segregation-of-duties comme propriétés attendues "dès l'architecture" — en tension directe avec TD-001 (25/08/2026, extension serverless plutôt que backend complet, PWA mono-utilisateur local, contrainte IT réelle).
- **Problem** : rouvrir TD-001 maintenant serait prématuré (aucun besoin réel multi-utilisateur démontré à ce jour). Mais fabriquer un **semblant** de RBAC/signature électronique purement local (ex. mot de passe par rôle stocké côté client) serait activement dangereux : cela fabriquerait une fausse preuve de conformité 21 CFR Part 11/Annexe 11, pire que son absence honnête.
- **Recommendation** : documenter explicitement cette limite (`00-cadrage-projet.md`) — l'outil, dans son état actuel, ne prétend pas satisfaire l'exigence e-signature/séparation des tâches multi-utilisateurs ; l'`audit_log` actuel (horodatage + acteur déclaré) est un premier niveau de traçabilité, pas une signature électronique réglementaire. Aucun RBAC/e-signature de façade construit. Un vrai service d'authentification serveur sera tranché quand un besoin réel (client multi-utilisateur) le démontrera.
- **Impact** : Aucun changement de code — clarification documentaire pure, protège contre une fausse assurance de conformité.
- **Reversibility** : Sans objet (décision documentaire) — TD-001 reste rouvrable plus tard sur besoin réel démontré.

---

### TD-012 — Prochaine capacité du Reasoning Engine : analyse d'impact de changement ancrée sur `QualityEvent`, pas un mode générique fourre-tout
- **Statut** : **ACTÉE** (revue panel E1-E7, 26/08/2026, `REVUE_PANEL_VISION_VALIDATION_ENGINEERING.md` Point C).
- **Context** : la vision détaille deux capacités IA non couvertes par les 4 outils actuels de Phase 15 : (1) analyse de qualité d'exigences (ambiguës/non-testables/doublons) ; (2) analyse d'impact de changement (Change → Composant → Requirement → Risk → Test → Evidence → Impact Assessment).
- **Problem** : construire les deux d'un coup répéterait l'erreur déjà évitée en Phase 13 (construire plusieurs capacités non éprouvées simultanément). La première (qualité d'exigences) suppose un texte source non structuré — relève de Source Intelligence (Phase 8a), pas du Reasoning Engine actuel qui lit des données déjà structurées.
- **Recommendation** : la prochaine capacité concrète du Reasoning Engine est une analyse d'impact de changement, directement câblable sur `QualityEvent` (Change Control, Phase 5)/`Requirement`/`Test`/`Evidence` déjà construits — prolonge exactement le scénario "changement de recette" déjà testé en Phase 15. L'analyse de qualité d'URS reste au backlog, dépendante de Source Intelligence, non engagée.
- **Impact** : Oriente le prochain incrément du Reasoning Engine après la Phase 17 (Mission workspace).
- **Reversibility** : Élevée — purement additif (nouveaux outils de lecture), aucune structure existante modifiée.

---

### TD-013 — Architecture Technique (PLC/SCADA/Server) : relations typées entre `AssetNode` existants, jamais une nouvelle entité d'équipement parallèle
- **Statut** : **ACTÉE** (revue panel E1-E7 condensée, 27/08/2026, `PHASE_18_ARCHITECTURE_TECHNIQUE_SPEC.md` §4, en réponse au document "VALIDAPHARM MASTER PRODUCT VISION / NORTH STAR").
- **Context** : la vision demande de modéliser Equipment/System/Subsystem/PLC/HMI/SCADA/Server/Database/Network/Software/Application/Interface et de tracer les relations belongs_to/controlled_by/connected_to/hosted_on/used_by entre eux.
- **Problem** : construire une nouvelle famille d'entités parallèle à `AssetNode` pour porter PLC/SCADA/Server dupliquerait un modèle d'équipement déjà générique et configurable — `AssetHierarchySchema.levels[]` (Phase 4/16) accepte déjà des niveaux nommés librement par le client (`level_key: string`, jamais une énumération figée dans le code), donc un client peut déjà créer des nœuds "PLC"/"SCADA"/"Serveur" sans aucun changement de code. Le vrai manquant est plus étroit : aucune relation *typée et dirigée* n'existe entre deux `AssetNode` (`associated_nodes[]` est un graphe libre non typé, incapable de distinguer "contrôlé par" de "connecté à").
- **Recommendation** : ajouter uniquement `RelationTechnique` (jointure explicite `AssetNode → AssetNode`, `type_relation: 'controle_par' | 'connecte_a' | 'heberge_sur'`, union de chaînes extensible sans migration), même pattern que `AssociationFonctionAssetNode`. Aucune nouvelle entité de nœud. Aucune détection de cycle (cohérent avec la tolérance déjà documentée d'`associated_nodes[]`).
- **Impact** : Structurant pour la Phase 18 — un seul ajout de table Dexie (v24), extension du Reasoning Engine avec un nouvel outil de traversal, aucune structure existante modifiée.
- **Reversibility** : Élevée — purement additif, aucune table existante modifiée, type de relation extensible sans migration future.

---

### TD-014 — Ingestion Office native : `jszip`+`DOMParser` pour la lecture `.docx`, Excel bloqué faute de librairie saine
- **Statut** : **ACTÉE** (recherche documentée, 27/08/2026, `PHASE_19_INGESTION_OFFICE_SPEC.md` §2, en réponse au chantier P0 "ingestion Office native" du plan `VISION_NORTH_STAR_CONVERGENCE.md`).
- **Context** : la vision exige de lire une procédure/un template `.docx`/`.xlsx` fournis directement par l'utilisateur — aucune librairie de parsing Office n'existait dans `package.json` avant cette phase.
- **Problem** : `mammoth` (candidat initial pour `.docx`) distingue son code Node de son code navigateur via le champ `browser` de son `package.json`, non appliqué par la résolution de modules de Vitest (SSR/Node) — installé et testé avec un `.docx` réel, l'erreur observée (`Could not find file in options`) a confirmé que le code exécuté en test n'était pas celui exécuté par la PWA réelle, malgré plusieurs tentatives de forcer la résolution (alias, `resolve.conditions`, `deps.inline`). `xlsx`/SheetJS (registre npm) porte une vulnérabilité haute (prototype pollution + ReDoS) sans correctif disponible sur npm (`npm audit`) ; `exceljs` introduit une dépendance transitive (`uuid`) avec une vulnérabilité modérée et une chaîne de dépendances datée.
- **Recommendation** : `.docx` — extraction directe du texte depuis `word/document.xml` avec `jszip` (dézippage, déjà 0 vulnérabilité) + `DOMParser` (API native, isomorphe Node/navigateur par construction, aucune ambiguïté de résolution). Excel — **aucune librairie installée** dans ce lot ; `xlsx_natif` n'est pas ajouté à `MethodeExtraction` tant qu'aucune solution saine n'est vérifiée (jamais une valeur de type sans implémentation réelle derrière). `docxtemplater`+`pizzip` (MIT, cœur gratuit, confirmé compatible navigateur) est retenu pour la future génération de documents au format client (Template Intelligence) — non installé dans ce lot, décision documentée par anticipation pour éviter une re-recherche.
- **Impact** : Structurant pour la Phase 19 — nouvelle dépendance `jszip` (production), `connecteurs/office/DocxNatifAdapter.ts`. Aucune capacité Excel dans ce lot (limite assumée, pas un oubli).
- **Reversibility** : Élevée — `jszip`+`DOMParser` est un choix additif ; réévaluer `mammoth` ou une librairie Excel reste possible sans migration si un correctif/une solution vérifiée apparaît.

---

### TD-015 — Une SOP n'est pas toujours du texte seul : images incorporées extraites séparément, filigrane/scan dégradé jamais "corrigé" algorithmiquement
- **Statut** : **ACTÉE** (27/08/2026, en réponse à une observation explicite de l'utilisateur sur la Phase 19 : "les SOP ne sont pas forcément qu'en Word, des fois c'est du Word avec des schémas et photos ou diagramme [...] et des fois c'est des SOP avec ou sans filigrane").
- **Context** : `extraireTexteDocx` (Phase 19) ne lisait que le texte de `word/document.xml`, ignorant silencieusement tout schéma/photo incorporé dans le `.docx` — une perte d'information potentiellement critique pour une procédure réelle. Par ailleurs, une SOP peut être un scan (image) portant un filigrane ("COPIE CONTRÔLÉE"/"DRAFT"), qui peut dégrader la qualité de l'OCR.
- **Problem** : ignorer les images incorporées reviendrait à lire une SOP en tronquant son contenu réel sans le signaler. À l'inverse, prétendre détecter/corriger algorithmiquement un filigrane sans preuve technique réelle serait une fabrication de capacité — exactement ce que la discipline "ne jamais fabriquer" interdit.
- **Recommendation** : `extraireImagesDocx` (nouvelle fonction, `connecteurs/office/DocxNatifAdapter.ts`) extrait chaque fichier `word/media/*` d'un `.docx`, avec son type MIME quand il est raster (png/jpeg/gif/bmp/tiff) — un format non raster (EMF/WMF, schéma Visio collé) est retourné explicitement avec `contentType: null`, jamais silencieusement omis. Ces images sont destinées à être transmises à l'OCR existant (`OcrRelayAdapter`, Phase 6) par un futur appelant, produisant une `Extraction` distincte sur la même `SourceVersion` (le modèle Phase 8a admet déjà plusieurs `Extraction` par version). Pour le filigrane/scan dégradé : **aucune détection/correction automatique construite** — le filet de sécurité reste la validation humaine déjà non négociable de tout `KnowledgeItem` (`a_valider` à la création). Documenté comme limite assumée, pas comme un problème résolu.
- **Impact** : Étend `DocxNatifAdapter.ts` (Phase 19) sans modifier son contrat existant (`extraireTexteDocx` inchangé). Aucune nouvelle dépendance.
- **Reversibility** : Élevée — purement additif.

---

### TD-016 — Cerveau procédural : structuration humaine d'une SOP versionnée, aucune extraction automatique de structure dans ce lot
- **Statut** : **ACTÉE** (revue panel E1-E7 condensée, 27/08/2026, `PHASE_20_PROCEDURAL_KNOWLEDGE_SPEC.md` §2/§4).
- **Context** : la vision (§12-15) demande de transformer une SOP en connaissance procédurale exécutable (étapes, conditions, exceptions, responsabilités), et de la suivre "comme le ferait un ingénieur humain".
- **Problem** : construire une extraction automatique de structure (texte libre → étapes) sans protocole de suggestion IA + confirmation humaine conçu et éprouvé fabriquerait une capacité non vérifiée — contraire à la discipline "ne jamais fabriquer" déjà appliquée à `KnowledgeItem` (Phase 8a, `valeur_interpretee` toujours fournie par l'appelant, jamais générée).
- **Recommendation** : construire `Procedure`/`ProcedureStep` — structure versionnée (même patron que `SourceVersion` : `reference` stable + `numero_version` auto-incrémenté, immuable) — remplie par un humain ayant lu la SOP (assistée par `extraireTexteDocx`/`extraireImagesDocx`, Phase 19). Un nouvel outil de lecture (`lister_etapes_procedure`) permet au Reasoning Engine de consulter cette structure. L'extraction automatique de structure reste un point ouvert, à trancher explicitement avec l'utilisateur (§7 de la spec) plutôt que décidée unilatéralement. Le suivi d'exécution/conformité (`ProcedureExecution`) est différé, même discipline que TD-009.
- **Impact** : Nouveau domaine "Procedure" (types + Dexie v25 + store), extension du Reasoning Engine (5ᵉ puis 6ᵉ outil de lecture). Aucune modification de structure existante.
- **Reversibility** : Élevée — purement additif ; une future extraction assistée par IA viendrait remplir cette même structure, sans la reconstruire.

---

### TD-017 — Structuration de SOP : chemin déterministe (sans IA) d'abord, repli IA seulement si la couverture réelle le réclame
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_21_PARSEUR_STRUCTURE_PROCEDURE_SPEC.md` §2/§4).
- **Context** : en réponse au point ouvert de TD-016, l'utilisateur confirme vouloir "tout faire d'abord sans l'IA" si possible, et signale que les SOP suivent en général un plan bien défini (objectif, scope, responsabilité, process) — en suggérant de vérifier sur des exemples réels dans Google Drive plutôt que de supposer.
- **Problem** : décider si l'IA est réellement nécessaire pour "lire, comprendre et suivre" une procédure sans preuve de terrain aurait été une affirmation non vérifiée dans un sens comme dans l'autre.
- **Recommendation** : deux SOP pharma réelles de deux clients différents (Sanofi Lyon "LYON-QUAL-PGN-000198", Ferring International Center "SMP-PROC-016168") ont été lues intégralement depuis Google Drive avant toute conception. Constat vérifié : les deux suivent le même enchaînement sémantique (objectif → périmètre → responsabilités → définitions → procédure → références → historique/annexes) sous un vocabulaire différent — un invariant suffisamment robuste pour un détecteur 100% déterministe (`detecterSections`/`proposerStructureProcedure`, aucun appel IA). Un troisième document réel du même corpus (SOP équipement Markem-Imaje, genre "instruction technique illustrée par tableaux d'étapes") sert de contre-exemple documenté : ce parseur ne le couvre pas correctement, limite assumée plutôt que cachée.
- **Impact** : nouveau module pur `src/logique-metier/procedures/parseurStructureProcedure.ts`, nouveaux types en mémoire (`SectionCanoniqueProcedure`/`SectionDetectee`/`EtapeProposee`/`PropositionStructureProcedure`, jamais persistés). Aucun changement de schéma Dexie, aucun nouvel outil de Reasoning Engine (ce n'est pas une capacité consultée par l'IA — c'est une étape de pré-traitement en amont de la confirmation humaine dans `useProcedureStore`).
- **Reversibility** : Élevée — purement additif ; un futur repli IA-assisté (pour les documents non couverts) viendrait consommer le même résultat `PropositionStructureProcedure`, sans le remplacer.

---

### TD-018 — Extension du parseur déterministe : reconnaissance par mot-clé, sous-titre comme contexte, repli ligne-par-ligne — maximiser la couverture sans IA avant tout repli
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_21_PARSEUR_STRUCTURE_PROCEDURE_SPEC.md` §8).
- **Context** : l'utilisateur, face à la démonstration d'un troisième genre de SOP réelle non couvert (voir Problem), demande explicitement de "concevoir ou étendre tous les champs possibles de parseur sans l'IA avant de passer au repli IA" — pousser le chemin déterministe à son maximum réel avant d'envisager la couche IA.
- **Problem** : sur demande de l'utilisateur ("as-tu un parseur capable de couvrir tous les types de SOP ?"), un troisième document réel du corpus Drive a été testé (IMA "4915BRP"/"LA1028BRP", procédures Back-up/Restore PLC/PC pour Ferring) — résultat concret et vérifié : 0 section canonique reconnue, 0 étape proposée. Ce document a un plan réel ("1. INTRODUCTION / 2. PLC Procedures / 2.1 Pre-requisites / ...") mais aucun libellé ne correspond exactement au dictionnaire TD-017, et ses instructions sont une phrase par ligne, sans puce ni numéro.
- **Recommendation** : trois extensions déterministes, chacune directement motivée par ce document réel, aucune spéculative :
  1. **Repli par mot-clé** (`MOTS_CLES_SECTIONS`) : quand le titre complet ne correspond à aucune clé exacte du dictionnaire, un mot fort à l'intérieur suffit ("PLC Procedures"/"PC Procedures (for HMI ima xface)" contiennent "Procedures" → `procedure` ; "INTRODUCTION" seul → `objectif`). Toujours un motif explicite testé, jamais un mot deviné — moins spécifique que la correspondance exacte (essayée en priorité), jamais moins déterministe.
  2. **Sous-titre comme contexte** (`RE_SOUS_TITRE`, `EtapeProposee.contexteDetecte`) : une ligne "N.M Titre" ("2.1 Pre-requisites") n'est jamais une nouvelle section ni une étape, mais son texte est retenu et attaché comme contexte aux étapes qui suivent — sans quoi deux procédures distinctes dans la même section ("Back-Up" vs "Restore") produiraient une liste plate indifférenciée.
  3. **Repli ligne-par-ligne** (`collecterLignesSection`) : une section `'procedure'` sans aucune ligne à puce/numéro (cas réel confirmé) utilise désormais chaque ligne non vide comme étape candidate, plutôt que de renvoyer une liste vide sur un document dont la structure est réelle mais moins formatée. Les puces/numéros explicites restent utilisés en priorité quand ils existent (haute confiance) — le repli ne s'active que s'ils sont absents.
- **Impact** : `EtapeProposee` gagne `contexteDetecte: string | null` (jamais persisté — même statut que le reste de `PropositionStructureProcedure`). Aucun changement de schéma Dexie. Retesté sur les deux SOP déjà couvertes (Sanofi/Ferring, TD-017) sans régression, puis sur le document IMA — désormais 3 sections reconnues (`objectif`/`procedure`/`procedure`) et des étapes extraites avec contexte.
- **Limite qui subsiste, non close par cette extension** : le genre "instruction technique illustrée par tableaux d'étapes" (Markem-Imaje, TD-017) reste hors couverture — ces extensions répondent au genre "procédure numérotée sans plan qualité", pas à celui-là. Une couverture déterministe "de tous les types de SOP" reste, par construction, hors de portée d'un système de règles : c'est la limite de fond documentée en TD-017, confirmée plutôt que contredite par cette extension.
- **Reversibility** : Élevée — purement additif, aucune régression sur le comportement déjà couvert (revérifié par test).

---

### TD-019 — Lecture de tableaux Word natifs (`extraireTableauxDocx`) : code maison plutôt qu'une librairie, aucune n'est saine
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_22_TABLEAUX_ET_DOCUMENT_INTELLIGENCE_SPEC.md` §1-2).
- **Context** : l'utilisateur demande explicitement de vérifier "dans tous les catalogues" (npm, seul écosystème pertinent, PWA 100% navigateur) l'existence d'outils pour "parser lire et comprendre des tableaux ou des étapes sous tableau" — le genre "instruction technique illustrée par tableaux d'étapes" (Markem-Imaje) laissé hors couverture par TD-017/TD-018.
- **Problem** : `officeparser` est Node uniquement (dépendances filesystem) ; `docx4js` n'a pas été publié depuis 2018 (même profil de risque que `xlsx`/`exceljs`, TD-014) ; `mammoth` est la librairie déjà abandonnée en Phase 19 (aucun fait nouveau ne justifie de rouvrir ce dossier) ; `docx`/`docx-templates` sont des générateurs, pas des lecteurs. Aucun candidat npm ne convient.
- **Recommendation** : étendre `DocxNatifAdapter.ts` (déjà vérifié, `jszip`+`DOMParser`, zéro vulnérabilité) avec `extraireTableauxDocx` — parcourt `<w:body>` dans l'ordre, capture chaque `<w:tbl>` de premier niveau en grille de cellules brutes (`lignes: string[][]`) et le texte du paragraphe non vide le plus proche qui le précède (`titreProchePrecedent`, un fait de position). Calibré et testé sur la structure réelle du manuel Markem-Imaje C350 (lu intégralement dans Google Drive) : tableaux "Previous achievement"/"Required time"/étape numérotée, répétés ~15 fois. `proposerEtapesDepuisTableaux` (nouveau) reconnaît une étape par première cellule exactement numérique, combine titre précédent et préconditions en `contexteDetecte`.
- **Impact** : `proposerStructureProcedure` accepte un paramètre `tableaux` optionnel, additif aux étapes textuelles — chaque tableau garde sa propre numérotation d'origine. `EtapeProposee`/`TableauDocx` étendus dans `domaine/types.ts`. Aucun changement de schéma Dexie (résultat toujours en mémoire, jamais persisté sans confirmation humaine — garde-fou TD-016 inchangé).
- **Reversibility** : Élevée — purement additif, aucune modification du texte extrait existant (`extraireTexteDocx` inchangé, juste refactoré pour partager du code).

---

### TD-020 — Azure AI Document Intelligence comme fournisseur OCR disponible (non activé) pour les tableaux dans un scan/PDF
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_22_TABLEAUX_ET_DOCUMENT_INTELLIGENCE_SPEC.md` §3).
- **Context** : `extraireTableauxDocx` (TD-019) ne s'applique qu'à un `.docx` natif — une SOP **scannée** avec des étapes en tableau n'a aucune couverture, et Azure AI Vision (Read API, fournisseur actif) ne renvoie que des lignes de texte plates, jamais de structure.
- **Problem** : reconstruire une structure de tableau depuis une image scannée par nos propres moyens (heuristiques de positionnement) serait un chantier de vision par ordinateur non éprouvé — contraire à "ne jamais fabriquer une capacité non vérifiée".
- **Recommendation** : Azure AI Document Intelligence (modèle `prebuilt-layout`) reconstruit nativement la structure ligne/colonne/cellule sur image *et* PDF — déjà anticipé dans le docstring de `FournisseurOcr` depuis TD-001 ("OCR/Document Intelligence"), exactement le rôle pour lequel cette interface a été conçue. `documentIntelligenceProvider.ts` l'implémente, même patron (soumission asynchrone + sondage) qu'`AzureVisionProvider`. Contrat REST écrit d'après connaissance de formation — `learn.microsoft.com`/`azure.microsoft.com` bloqués par le proxy réseau de cette session, impossible à revérifier aujourd'hui ; même réserve explicite déjà présente dans `azureVisionProvider.ts` depuis TD-001 ("à revérifier au déploiement réel").
- **Impact** : `ResultatExtractionOcr` (Worker) gagne un champ `tableaux?: TableauOcr[]` optionnel — `undefined` pour un fournisseur qui n'en fournit pas (Azure Vision), jamais un tableau vide fabriqué. **Non câblé comme fournisseur actif** (`index.ts` reste sur `AzureVisionProvider`) — remplacer/ajouter un fournisseur est un choix de compte Azure (quota, coût potentiel) qui revient explicitement à l'utilisateur, jamais une bascule silencieuse. `ocrHandler.ts` continue de rejeter tout `Content-Type` non `image/*` — router un PDF vers ce fournisseur demanderait de l'assouplir, non fait dans ce lot.
- **Reversibility** : Élevée — purement additif, `AzureVisionProvider` inchangé et toujours actif.

---

### TD-021 — Ingestion PDF native : `pdfjs-dist` (build `legacy`), calibrée sur un 4ᵉ genre réel de SOP
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_23_INGESTION_PDF_SPEC.md`).
- **Context** : l'utilisateur demande explicitement "Fais les deux [ingestion PDF + repli IA]. Tu trouves pas des sop en pdf dans Google drive ? Ouvre tous les docs pharma il y'a sûrement un pdf" — fermer la limite "aucune ingestion PDF réelle" déjà signalée en Phase 22.
- **Problem** : `pdfjs-dist` était déjà identifié comme candidat en Phase 22 mais non testé. Une recherche élargie dans Drive (`mimeType = 'application/pdf'`) fait remonter un **quatrième genre réel** de SOP jamais rencontré ("PB1D_Operation.pdf", manuel Nordson — en-têtes en gras sans aucune numérotation), confirmant sur un document indépendant supplémentaire que la limite de fond (TD-017/TD-018) tient.
- **Recommendation** : `pdfjs-dist` (0 vulnérabilité `npm audit`) retenu, mais testé réellement plutôt que présumé fonctionnel : le build principal échoue à l'import en environnement de test (`DOMMatrix is not defined`, API Canvas absente de jsdom) ; le build `legacy` (documenté par Mozilla pour les environnements sans Canvas complet) importe et exécute sans erreur dans les deux environnements. Différence assumée avec l'échec de `mammoth` (TD-014) : ici `getDocument`/`getTextContent` sont le même code, octet pour octet, en test et en production — seule l'URL du worker/des polices diffère (injectée en paramètre, résolution `import.meta.url` par défaut, native Vite en navigateur réel), une configuration d'environnement, jamais une divergence de logique comme l'était le bug `mammoth`.
- **Impact** : `extraireTextePdf` (`src/connecteurs/pdf/PdfNatifAdapter.ts`), `MethodeExtraction` étendu avec `pdf_natif`. Testé sur un PDF réellement valide construit à la main (table `xref` à offsets exacts) — un bug réel trouvé et corrigé pendant la construction du test (troncature de texte sur une `MediaBox` trop étroite). Aucune extraction de tableau PDF natif (limite assumée — `getTextContent` ne reconstruit pas de structure, le repli scanné reste `DocumentIntelligenceProvider`, TD-020). Aucun wiring écran/store dans ce lot.
- **Reversibility** : Élevée — purement additif, aucun changement à `extraireTexteDocx`/`extraireTableauxDocx`.

---

### TD-022 — Repli IA-assisté de structuration procédurale : vérification déterministe d'ancrage plutôt que citation vers un objet existant
- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_24_REPLI_IA_STRUCTURE_PROCEDURE_SPEC.md`).
- **Context** : point explicitement laissé ouvert par TD-016 ("à trancher explicitement avec l'utilisateur avant de l'engager") — tranché en deux temps par l'utilisateur : "tout faire d'abord sans l'IA" (TD-017), puis, une fois la couverture déterministe testée sur 4 genres réels distincts (5 documents : Sanofi, Ferring×2, IMA, Markem-Imaje couverts ; Nordson non couvert, TD-021), "Fais les deux [PDF + repli IA]" — autorisation explicite d'engager ce repli.
- **Problem** : le mécanisme de citation déterministe du Reasoning Engine (Phase 15) vérifie qu'un identifiant cité résout vers un objet du domaine déjà obtenu par appel d'outil — inapplicable ici, puisqu'il n'existe encore aucun objet persisté à citer : la structure elle-même est ce qui est proposé. Accorder une confiance à une proposition d'IA sur sa seule auto-évaluation contredirait le principe fondateur n°1 et l'invariant #8 ("AI output is not canonical by confidence alone").
- **Recommendation** : `proposerStructureProcedureParIA` (`src/logique-metier/procedures/proposerStructureProcedureIA.ts`) réutilise l'infrastructure `ProviderAdapter` déjà éprouvée (chat expert, §4.4) — aucune nouvelle interface fournisseur. Le prompt exige explicitement que le modèle **recopie** le texte source mot pour mot. Une **vérification déterministe d'ancrage** (nouvelle, propre à ce lot) compare chaque section/étape proposée, texte normalisé, au document source : présente → `EtatConfianceIA` `'infere'` ; absente (reformulation ou hallucination) → `'a_verifier'`, jamais un état plus confiant sur la seule affirmation du modèle. Protocole de sortie contraint (`SECTION|.../ETAPE|...`, une ligne = un marqueur) plutôt que du JSON libre — même discipline que le protocole texte du Reasoning Engine (Phase 15) : parsing déterministe, ligne malformée ignorée plutôt qu'un plantage.
- **Impact** : nouveau module pur `proposerStructureProcedureIA.ts`, nouveaux types en mémoire (`SectionDetecteeIA`/`EtapeProposeeIA`/`PropositionStructureProcedureIA`, jamais persistés). Aucun changement de schéma Dexie, aucun nouveau contrat fournisseur IA. Garde-fou TD-016 inchangé : le résultat n'est jamais écrit dans `Procedure`/`ProcedureStep` sans confirmation humaine.
- **Limite assumée** : la vérification est un ancrage de sous-chaîne normalisée (casse, espaces), pas une similarité sémantique floue — une reformulation même légère est honnêtement marquée `'a_verifier'`, jamais acceptée à tort ; aucun déclenchement automatique ni écran de revue construits dans ce lot.
- **Reversibility** : Élevée — purement additif, aucune modification du parseur déterministe (Phases 21-22) ni de l'infrastructure `ProviderAdapter` existante.

---

### TD-023 — Déclenchement automatique du repli IA (uniquement si le déterministe trouve strictement rien) + écran de revue humaine

- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_25_ECRAN_REVUE_STRUCTURE_PROCEDURE_SPEC.md`).
- **Context** : dernier point ouvert laissé par TD-022 ("aucun déclenchement automatique ni écran de revue construits dans ce lot") — l'utilisateur confirme explicitement ("D'accord continue") vouloir clore ce point maintenant que la couverture déterministe (Phases 21-23) et le repli IA (Phase 24) existent tous deux comme fonctions appelables mais isolées.
- **Problem** : sans orchestration, un appelant devrait décider lui-même quand appeler le parseur déterministe puis, éventuellement, le repli IA — au risque d'appeler l'IA même quand le déterministe a déjà trouvé une structure exploitable (coût, latence, et surtout un mélange implicite de provenance entre sections/étapes déterministes et IA dans un même résultat, dont la traçabilité deviendrait ambiguë pour l'humain qui confirme).
- **Recommendation** : nouvelle fonction pure d'orchestration `proposerStructureProcedureAvecRepli` (`src/logique-metier/procedures/proposerStructureProcedureAvecRepli.ts`) — appelle toujours le parseur déterministe (Phases 21-22) en premier, et n'invoque le repli IA (Phase 24) que si celui-ci ne trouve **strictement rien** (zéro section ET zéro étape). Le résultat est un type discriminé `PropositionAvecSource` (`source: 'deterministe' | 'ia'`) : jamais un mélange des deux sources dans une même proposition, `source` désigne toujours sans ambiguïté la provenance de la totalité du résultat. Un nouvel écran `RevueStructureProcedure.vue` (route `/clients/:clientId/procedures`, lien sidebar "Procédures") permet de coller du texte ou d'importer un `.docx`/`.pdf` (réutilise `extraireTexteDocx`/`extraireTableauxDocx`/`extraireTextePdf` existants), déclenche la génération, affiche les sections/étapes proposées (badge de confiance IA réutilisant le style `.badge-confiance`/`LIBELLES_CONFIANCE` déjà établi par `MissionWorkspace.vue`), permet de retenir/exclure/éditer chaque étape avant confirmation, et n'écrit `Procedure`/`ProcedureStep` qu'après confirmation humaine explicite (garde-fou TD-016 inchangé).
- **Impact** : `useProcedureStore` étendu (`derniereProposition`, `genererProposition`, `annulerProposition`, `confirmerProposition` — jamais d'écriture directe hors `confirmerProposition`, qui délègue à `creerProcedure`/`ajouterEtape` déjà existants). Nouvelle route/lien sidebar. Aucun changement de schéma Dexie (la proposition reste en mémoire, jamais persistée telle quelle).
- **Vérification navigateur réelle** (Playwright, `pwbrowsers/chromium` préinstallé) : parcours complet testé en conditions réelles — (1) chemin déterministe : texte à en-têtes numérotés → proposition affichée → confirmation → `Procedure`/`ProcedureStep` réellement créés et visibles dans la liste persistée de l'écran ; (2) annulation : proposition générée puis annulée → aucune écriture, écran réinitialisé ; (3) repli IA sans fournisseur configuré → erreur réseau propre affichée à l'utilisateur (jamais un plantage silencieux), même discipline que `PanneauChat`/`MissionWorkspace`.
- **Reversibility** : Élevée — purement additif ; `proposerStructureProcedure`/`proposerStructureProcedureParIA` restent inchangés et appelables indépendamment.

---

### TD-024 — Gabarits d'export personnalisés client : équivalence de contenu par construction, `docxtemplater`+`pizzip` réellement installés et testés

- **Statut** : **ACTÉE** (revue condensée, 27/08/2026, `PHASE_26_GABARITS_EXPORT_CLIENT_SPEC.md`).
- **Context** : URS-F-023 à 026 (§4.3bis) spécifient ce besoin depuis longtemps ; `ClientConfig.export_template_id` existe depuis les premières versions du modèle sans jamais avoir été exploité. `docxtemplater`+`pizzip` avaient été pré-choisis par anticipation en TD-014 (Phase 19) sans être installés ni testés. L'utilisateur a demandé de continuer sur le plan documenté après la clôture de la Phase 25.
- **Problem** : produire un document dans le format natif exact d'un client (pas l'astuce "HTML encapsulé en `.doc`" utilisée pour le gabarit par défaut) tout en garantissant que son contenu reste identique à celui du gabarit par défaut (URS-F-025) — sans dupliquer la logique de lecture des données de `Section`/`DefinitionGabarit` entre deux renderers, ce qui laisserait la porte ouverte à une divergence future.
- **Recommendation** : `construireDonneesExportGabarit` (`logique-metier/export/donneesExportGabarit.ts`) devient l'unique fonction lisant `Section`/`DefinitionGabarit` — `genererExportWord` (HTML, refactoré sans changement de sortie, vérifié par la suite de tests existante) et `genererDocxPersonnalise` (nouveau, `.docx` réel) consomment tous deux sa sortie : l'équivalence de contenu est garantie **par construction**, jamais par une comparaison de checksum a posteriori (corrige le modèle spéculatif `export_template`/`checksum_semantique_reference` esquissé en FS v17, jamais implémenté tel quel). `docxtemplater`+`pizzip` réellement installés, recherche re-vérifiée (npm registry + `npm audit` réels, pas seulement les métadonnées consultées en TD-014) : 0 vulnérabilité, licences MIT. `verifierGabaritExportClient` refuse à l'import tout gabarit ne mappant pas les balises obligatoires (`redacteurs`/`approbateur_final`/`historique_revisions`, URS-F-026) — jamais enregistré "à corriger plus tard".
- **Bug réel trouvé et corrigé pendant la construction du test** : le comportement par défaut de `docxtemplater` pour une balise absente des données au moment du rendu est d'écrire littéralement le texte `"undefined"` dans le document généré (jamais une erreur, jamais une chaîne vide) — un tel texte serait apparu tel quel dans un livrable réglementaire. `nullGetter: () => ''` neutralise ce défaut.
- **Écart type/API réel documenté honnêtement** : `Docxtemplater.getTags()` existe et fonctionne réellement à l'exécution (vérifié par test) mais n'est pas déclaré dans les types TypeScript publiés de la librairie — un type d'appoint local comble l'écart, avec commentaire justificatif, plutôt que de masquer l'erreur `tsc` par une suppression générique (même discipline que le retrait honnête d'une option `pdfjs-dist` inexistante en TD-021).
- **Impact** : nouveau type domaine `GabaritExportClient`, table Dexie `gabaritsExportClient` (version 26 du schéma), `useGabaritExportStore`, extension de `EditeurSection.vue`. `genererExportWord.ts` refactoré (comportement HTML inchangé, vérifié).
- **Limite assumée** : portée livrée limitée au format `.docx` — PDF/Excel personnalisés (mentionnés par URS-F-023) restent hors périmètre, aucune librairie de génération saine identifiée pour ces deux formats à ce jour (même discipline que le blocage Excel de TD-014).
- **Reversibility** : Élevée — purement additif ; `genererExportWord` conserve son comportement HTML existant, `GenerationDocxAdapter.ts` est un module indépendant.

---

### TD-025 — Context Engine enrichi : narratif OÙ/QUOI/COMMENT/POURQUOI-IMPACT + grounding réel du Reasoning Engine

- **Statut** : **ACTÉE** (revue condensée + choix explicite de l'utilisateur entre 3 options, 27/08/2026, `PHASE_27_CONTEXT_ENGINE_ENRICHI_SPEC.md`).
- **Context** : l'utilisateur demande d'entamer le chantier P1 suivant du plan `VISION_NORTH_STAR_CONVERGENCE.md` ("Context Engine enrichi (narratif WHY/WHAT/WHERE/HOW/IMPACT)"). En creusant l'implémentation réelle (`useReasoningEngineStore.executerRaisonnement`) avant toute conception, un fait structurant a été découvert : `contextSnapshotId` n'était **jamais** consommé pour construire le prompt envoyé au fournisseur IA — uniquement persisté sur `AIRequest.context_snapshot_id` à des fins de traçabilité. Le Context Engine (Phase 14) et le Reasoning Engine (Phase 15) étaient donc déconnectés en pratique, malgré leur câblage apparent dans `MissionWorkspace.vue` (Phase 17).
- **Recherche du concept source** : la formulation "narratif WHY/WHAT/WHERE/HOW/IMPACT" ne provient d'aucun document relu dans Google Drive à ce jour (le document "MASTER PRODUCT VISION / NORTH STAR" cité par `VISION_NORTH_STAR_CONVERGENCE.md` n'a pas été retrouvé dans Drive — probablement transmis directement par l'utilisateur lors d'un tour antérieur non conservé). Le concept le plus proche réellement localisé est `01_ARCHITECTURE_MASTER_FINAL.md`/`04_RELATIONSHIP_MATRIX_FINAL.md` (dossier `10- Architecture détaillée`, déjà lu intégralement en Phase 0) : Context = FACTS, Method/SOP = HOW, Rules/Risk/Requirements/History = WHY/CONSEQUENCES — utilisé comme fondement plutôt que fabriqué depuis le seul intitulé du plan.
- **Décision de portée, explicitement soumise à l'utilisateur** : 3 options présentées (narratif+grounding réel / narratif seul UI / autre chantier P1) — l'utilisateur choisit "Narratif + grounding réel", acceptant explicitement le changement de comportement du Reasoning Engine déjà validé en Phase 15.
- **Recommendation** : `construireNarratifContexte` (`logique-metier/contexte/narratifContexteSnapshot.ts`) réorganise les `ContextSnapshotItem` déjà résolus par `assemblerElementsContextSnapshot` (Phase 14, aucune nouvelle résolution) en quatre facettes : OÙ (`asset_node`), QUOI (`manufacturing_context`), COMMENT (`Procedure` — toujours vide, voir limite), POURQUOI/IMPACT (`quality_event`). `construirePrompt` (Phase 15) accepte un narratif sérialisé optionnel, préfixé avant l'objectif. `executerBoucleRaisonnement` ajoute les identifiants du narratif à `idsConnus` **dès le premier tour** — même garantie qu'un appel d'outil (données déterministes déjà résolues à l'assemblage du snapshot), jamais une confiance accrue sur la seule affirmation du modèle.
- **Gap corrigé au passage** : `MissionWorkspace.vue` codait en dur `manufacturingContexts: []` lors de l'assemblage du contexte (`useProcessContextStore` jamais importé) — corrigé, la facette QUOI est désormais réellement peuplée quand des `ManufacturingContext` existent.
- **Impact** : `protocoleRaisonnement.ts`/`boucleRaisonnement.ts` étendus (paramètre optionnel, rétrocompatibles — testés sans régression). `useReasoningEngineStore.executerRaisonnement` charge `manufacturingContexts`/`qualityEvents`/`contextSnapshotItems` en plus des données déjà chargées. `MissionWorkspace.vue` : rendu en facettes remplace la liste plate.
- **Limite assumée** : la facette COMMENT reste vide (`Procedure` sans rattachement `AssetNode`/`Workspace`, limite déjà actée en TD-016). **Vérification navigateur réelle limitée à la facette OÙ** — aucun écran de création n'existe pour `ManufacturingContext` ni `QualityEvent` dans toute l'application (gap préexistant, découvert en préparant la vérification, non introduit ni corrigé par ce lot) ; QUOI et POURQUOI/IMPACT sont vérifiées uniquement par test unitaire de la fonction pure.
- **Reversibility** : Élevée — tous les nouveaux paramètres sont optionnels ; en leur absence, le comportement du Reasoning Engine est strictement identique à avant ce lot.

---

### TD-026 — Deliverable Intelligence : calcul automatique de `readiness` depuis la chaîne réelle de traçabilité

- **Statut** : **ACTÉE** (revue condensée + choix explicite de l'utilisateur entre 3 options, 28/08/2026, `PHASE_28_DELIVERABLE_READINESS_SPEC.md`).
- **Context** : l'utilisateur demande d'entamer le chantier P1 suivant du plan `VISION_NORTH_STAR_CONVERGENCE.md`. Investigation de `09_DELIVERABLE_ENGINE.md` (Drive) et du code existant (`ContentPlan.readiness`, `types.ts`) : un commentaire explicite du type domaine, posé en Phase 9 (v26), documente que `readiness` est *"fourni explicitement par l'appelant à la création, jamais calculé automatiquement par ce module (pas de mécanisme d'évaluation de complétude construit ici)"* — un gap structurellement identique à celui découvert pour le Context Engine en TD-025 (mécanisme documenté mais jamais construit).
- **Décision de portée, explicitement soumise à l'utilisateur** : 3 options présentées (calcul de readiness / détermination des livrables requis / autre chantier P1) — l'utilisateur choisit "Calcul de readiness", le plus petit incrément réel et le mieux borné des trois (la chaîne `Requirement→Couverture→Test→Execution→Evidence` existe et est déjà testée depuis les Phases 7a/7b/7c).
- **Problem** : déterminer, sans rien fabriquer ni deviner, si les données déjà résolues pour un `ContentPlan` (ancré sur `asset_node_id`) suffisent à générer un livrable — sans dupliquer la logique de résolution déjà utilisée par l'outil `lister_requirements_pour_actif` du Reasoning Engine (Phase 15) ni par `assemblerElementsContextSnapshot` (Phase 14).
- **Recommendation** : `construireReadinessContentPlan` (`logique-metier/deliverable/readinessContentPlan.ts`), fonction pure, résout `Requirement → Couverture → Test → Execution → Evidence` ancrée sur `asset_node_id`, plus un `QualityEvent` non clôturé sur ce même nœud (toujours bloquant). Combine plusieurs signaux par sévérité pire-cas (`bloque` > `besoin_information` > `besoin_revue` > `pret`) — jamais une moyenne, jamais le premier trouvé, même style que `grilleDecision.ts`/`verifierBlocageExport.ts` (Phase 1) déjà établis. `asset_node_id` null ou aucun `Requirement` rattaché → toujours `besoin_information`, jamais un `pret` deviné. `useContentPlanStore.creerContentPlan` calcule désormais `readiness` automatiquement (retire `readiness` de `NouveauContentPlanInput`) ; nouvelle fonction `recalculerReadiness(clientId, contentPlanId)` recharge la chaîne et recalcule à la demande, tracée dans `audit_log`, refusée sur un plan `gele`.
- **Vérification de source, honnête** : un agent dédié a relu intégralement `01_ARCHITECTURE_MASTER_FINAL.md` (12332 caractères, non tronqué) pour vérifier la sémantique exacte de §26 avant de finaliser cette décision. Résultat : §26 ("Deliverable Engine") définit uniquement la **position** de l'étape "Readiness" dans le pipeline (`... → Context Snapshot → Readiness → Content Plan → ...`) — les états `READY`/`NEEDS_INFORMATION`/`NEEDS_REVIEW`/`BLOCKED` eux-mêmes **n'apparaissent nulle part** dans ce document (recherche exhaustive du texte : seul `NEEDS_REVIEW` apparaît, dans une section distincte, §20 "Diagram Intelligence", sans rapport avec `ContentPlan`). Ces 4 états proviennent du type domaine `ReadinessContentPlan` déjà posé en Phase 9 (v26) — cette phase en construit le calcul, elle n'en invente ni n'en corrige la sémantique, et ne prétend pas citer §26 pour autre chose que la position dans le pipeline.
- **Impact** : nouveau module pur `readinessContentPlan.ts` (14 tests). `useContentPlanStore.ts` : signature de `NouveauContentPlanInput` changée (`readiness` retiré, appel cassant assumé — aucun autre appelant dans le code que le store et ses tests, vérifié par recherche exhaustive), nouvelle fonction exportée `recalculerReadiness`.
- **Limite assumée** : **toujours aucun écran `ContentPlan`** dans l'application (limite déjà documentée en Phase 9, jamais close depuis) — vérification en navigateur réel impossible dans ce lot, seule la suite de tests (14 tests du module pur + 11 tests du store, dont 4 nouveaux) couvre ce module.
- **Reversibility** : Élevée — `NouveauContentPlanInput.readiness` retiré est un changement cassant d'API interne, mais sans aucun autre consommateur ; `construireReadinessContentPlan` est une fonction pure additive, aucune modification des Phases 7a/7b/7c/9 sous-jacentes.

---

### TD-027 — Risk Assessment (AMDEC) autonome : méthodologie client généralisée, calquée sur un modèle réel

- **Statut** : **ACTÉE** (choix explicite de l'utilisateur entre 3 chantiers P1, 28/08/2026, `PHASE_29_RISK_ASSESSMENT_AMDEC_SPEC.md`).
- **Context** : l'utilisateur demande de traiter les 3 chantiers P1 restants du plan Vision North Star dans l'ordre "1 puis 2 et 3 sans t'arrêter sauf besoin d'une décision", en choisissant lui-même l'ordre après une `AskUserQuestion` présentant Risk Assessment (AMDEC), Compliance Engine généralisé et Knowledge Graph générique. Risk Assessment est retenu en premier : dette technique documentée trois fois indépendamment (`CURRENT_ARCHITECTURE.md` "AMDEC non autonome", `LEGACY_MAPPING.md` "EXTEND son point d'ancrage... demain rattachable à un vrai module AMDEC autonome") — `calculerIPR.ts` (S×O×D, Phase 1) n'existait jusqu'ici que comme colonne calculée imbriquée dans le gabarit DQ, jamais comme module de risque indépendant.
- **Problem** : généraliser l'AMDEC en un module autonome, avec une méthodologie versionnée par client (échelle S/O/D, seuil d'action) comme `MethodProfileACFC`/`MethodProfileImpactAssessment` (Phase 1/3) déjà établis — sans réinventer le calcul IPR lui-même (déjà correct et testé) ni deviner une structure d'AMDEC non grounded.
- **Recherche du concept source** : une recherche Google Drive (`fullText contains 'AMDEC'`) retrouve `Processus_AMDEC.xlsx`, un modèle AMDEC réel (générique, non spécifique pharma) confirmant la structure exacte à répliquer : Étape du processus / Mode de défaillance / Effet / SEV / Cause / OCC / Contrôle actuel / DET / RPN (= IPR, formule identique à `calculerIPR`), puis Recommandations / Responsable et date cible / Actions menées / SEV-OCC-DET-RPN **résiduels** — un cycle en deux temps (évaluation initiale → action → évaluation résiduelle) absent de tout ce qui existait dans le dépôt avant ce lot.
- **Recommendation** : `MethodProfileRiskAssessment` (`echelle_min`/`echelle_max`/`seuil_action` configurables par client, versionné, immuable) + `RiskAssessment` (ligne AMDEC, rattachable à `AssetNode`/`Parameter`) dans `logique-metier/domaine/types.ts`. `evaluerVerdictRiskAssessment` (`logique-metier/risque/`), fonction pure, compare l'IPR déjà calculé par `calculerIPR` (**KEEP**, réutilisé tel quel, aucune seconde implémentation) au seuil de la méthode — `null` explicite si l'IPR n'est pas calculable, jamais un verdict deviné. `useRiskAssessmentStore` : `creerNouvelleVersion`/`creerEvaluation` (même patron exact que `useImpactAssessmentStore`) + nouvelle fonction `enregistrerActionResiduelle` (recommandation/responsable/date cible/actions menées, recalcule S/O/D/IPR résiduels), refusée sur un `RiskAssessment` introuvable.
- **Impact** : nouveau module pur `evaluerVerdictRiskAssessment.ts` (5 tests), nouveau store `useRiskAssessmentStore.ts` (9 tests), table Dexie `methodProfilesRiskAssessment`/`risksAssessment` (schéma v27). `calculerIPR.ts`/gabarit DQ inchangés — aucune régression, EXTEND pur (point d'ancrage additionnel, pas un remplacement).
- **Limite assumée** : aucun écran construit dans ce lot (domaine + persistance + store seulement, même discipline que les Phases 5/8a/9/13) ; le rattachement `Parameter`/`CPP` reste optionnel, aucune promotion automatique d'un verdict AMDEC vers un `CPP`/`CQA` (cohérent avec le garde-fou DEC-019 déjà établi en Phase 2).
- **Reversibility** : Élevée — purement additif, `calculerIPR.ts` et le gabarit DQ restent inchangés et testés sans régression.

---

### TD-028 — Compliance Engine généralisé : factorisation du patron règle→blocage (règle de trois)

- **Statut** : **ACTÉE** (choix explicite de l'utilisateur entre 3 options, 28/08/2026, `PHASE_30_COMPLIANCE_ENGINE_SPEC.md`).
- **Context** : deuxième des 3 chantiers P1 restants. Aucun document source ne précise l'ensemble des règles cibles d'un "Compliance Engine" au-delà d'un seul exemple (`VISION_NORTH_STAR_CONVERGENCE.md` §3, couche 14 : *"verifierBlocageExport.ts (un seul statut vérifié) — à généraliser"*). Inventer de nouvelles règles métier sans source réelle aurait été une fabrication — écarté explicitement.
- **Décision de portée, explicitement soumise à l'utilisateur** : 3 options présentées — factoriser le patron commun déjà répété 3 fois indépendamment dans le code (`verifierBlocageExport.ts` Phase 1, `gardesFinalisation.ts` U-01/U-02/U-03, `gelerContentPlan` readiness gate Phase 28) / passer directement au chantier 3 (Knowledge Graph) / une règle de conformité concrète fournie par l'utilisateur. L'utilisateur choisit "Factoriser le patron commun" (règle de trois pleinement satisfaite : 3 implémentations indépendantes de la même forme "évaluer un ensemble de règles contre un contexte, retourner les règles bloquantes").
- **Recommendation** : `evaluerReglesConformite<TContexte, TCode>` (`logique-metier/conformite/evaluerReglesConformite.ts`), fonction pure générique — prend un contexte et un tableau de `RegleConformite` (`{ code, bloque, message }`), retourne les règles bloquantes dans l'ordre de déclaration (jamais un court-circuit, plusieurs règles indépendantes pouvant s'appliquer simultanément, ex. U-01+U-02 à l'entrée en vérification d'une section IQ). **Refactor pur, comportement strictement identique** : les 3 consommateurs conservent leur API publique et leurs tests déjà validés inchangés — `verifierBlocageExport` et `gardesFinalisation.evaluerGardesFinalisation` gardent exactement leur signature, `useContentPlanStore.gelerContentPlan` garde exactement `{ erreur: 'donnees_non_pretes' }`. Aucune nouvelle règle métier créée.
- **Impact** : nouveau module pur `evaluerReglesConformite.ts` (4 tests). `verifierBlocageExport.ts`/`gardesFinalisation.ts`/`useContentPlanStore.ts` : implémentation interne réécrite pour consommer ce moteur, **aucune de leurs suites de tests existantes n'a été modifiée** — toutes restent vertes sans changement, preuve directe de la non-régression comportementale.
- **URS/FS non modifiées** : ni l'un ni l'autre ne référence l'implémentation interne (`verifierBlocageExport.ts`/`gardesFinalisation.ts` par leur nom) — seul le comportement observable compte pour ces documents, strictement inchangé.
- **Limite assumée** : ce moteur n'est pas branché sur un 4ᵉ consommateur dans ce lot — reste un outil disponible pour de futures règles de blocage, jamais imposé rétroactivement à un mécanisme qui n'en a pas besoin.
- **Reversibility** : Élevée — refactor interne pur, aucune API publique modifiée, régression vérifiée par la suite de tests existante restée inchangée.

---

*Dernier livrable de la Phase 0 : `CONVERGENCE_PLAN.md` (ordre de mise en œuvre synthétisant les 6 documents précédents). TD-007 à TD-009 ajoutées le 26/08/2026, hors Phase 0 — revue panel dédiée déclenchée par une clarification de vision produit de l'utilisateur. TD-010 à TD-012 ajoutées le 26/08/2026 — revue panel déclenchée par un second document de vision approfondi ("Validation Engineering Platform") accompagné d'un benchmark UX concurrent réel. TD-013 ajoutée le 27/08/2026 — revue panel déclenchée par un troisième document de vision ("MASTER PRODUCT VISION / NORTH STAR") et sa checklist d'audit de capacités associée, ouvrant le plan de convergence `VISION_NORTH_STAR_CONVERGENCE.md`. TD-014 ajoutée le 27/08/2026 — recherche de librairie d'ingestion Office (Phase 19), `mammoth` évalué puis abandonné (incompatibilité de résolution Node/navigateur), Excel bloqué faute de librairie saine (vulnérabilités non corrigées). TD-015 ajoutée le 27/08/2026 — extension de la Phase 19 suite à une observation explicite de l'utilisateur sur la robustesse de lecture (images incorporées, filigrane). TD-016 ajoutée le 27/08/2026 — cerveau procédural (Phase 20) : structuration humaine versionnée d'une SOP, extraction automatique de structure explicitement laissée en point ouvert. TD-017 ajoutée le 27/08/2026 — parseur déterministe de structure procédurale (Phase 21), calibré sur 2 SOP réelles de clients différents consultées dans Google Drive avant conception. TD-018 ajoutée le 27/08/2026 — extension du même parseur (mot-clé, contexte de sous-titre, repli ligne-par-ligne) sur demande explicite de l'utilisateur de maximiser la couverture sans IA, motivée par un 3ᵉ document réel (IMA) initialement non couvert. TD-019/TD-020 ajoutées le 27/08/2026 (Phase 22) — recherche de catalogue npm pour la lecture de tableaux (aucune librairie saine trouvée, code maison retenu), `extraireTableauxDocx`/`proposerEtapesDepuisTableaux` calibrés sur le manuel Markem-Imaje réel, et `DocumentIntelligenceProvider` (Azure) rendu disponible mais non activé pour les tableaux dans un scan/PDF. TD-021 ajoutée le 27/08/2026 (Phase 23) — ingestion PDF native (`pdfjs-dist`, build `legacy`), calibrée sur un 4ᵉ genre réel de SOP découvert dans Drive (manuel Nordson, en-têtes en gras non numérotés). TD-022 ajoutée le 27/08/2026 (Phase 24) — repli IA-assisté de structuration procédurale, point ouvert depuis TD-016 explicitement engagé par l'utilisateur ; vérification déterministe d'ancrage du texte proposé dans le document source, jamais une confiance sur la seule affirmation du modèle. TD-023 ajoutée le 27/08/2026 (Phase 25) — orchestration déterministe-puis-repli-IA (`proposerStructureProcedureAvecRepli`) et écran de revue humaine (`RevueStructureProcedure.vue`), clôturant le dernier point ouvert de TD-022. TD-024 ajoutée le 27/08/2026 (Phase 26) — gabarits d'export personnalisés client (URS-F-023 à 026 enfin implémentées), `docxtemplater`+`pizzip` réellement installés et testés (pré-choisis par anticipation TD-014), équivalence de contenu garantie par construction plutôt que par checksum a posteriori. TD-025 ajoutée le 27/08/2026 (Phase 27) — Context Engine enrichi : narratif OÙ/QUOI/COMMENT/POURQUOI-IMPACT et grounding réel du Reasoning Engine, corrigeant un gap découvert en cours de conception (`contextSnapshotId` jamais consommé par le prompt IA jusqu'ici) ; portée choisie explicitement par l'utilisateur parmi 3 options proposées. TD-026 ajoutée le 28/08/2026 (Phase 28) — Deliverable Intelligence : calcul automatique de `readiness` (`ContentPlan`) depuis la chaîne réelle Requirement→Couverture→Test→Execution→Evidence + QualityEvent bloquant, corrigeant un gap structurellement identique à TD-025 (`readiness` documenté "jamais calculé" depuis Phase 9) ; portée choisie explicitement par l'utilisateur parmi 3 options proposées ; source `01_ARCHITECTURE_MASTER_FINAL.md` §26 relue intégralement, ne définit que la position du pipeline, pas la sémantique des états. TD-027 ajoutée le 28/08/2026 (Phase 29) — Risk Assessment (AMDEC) autonome, corrigeant la dette technique "AMDEC non autonome" documentée depuis `CURRENT_ARCHITECTURE.md`/`LEGACY_MAPPING.md` ; structure calquée sur un modèle AMDEC réel trouvé dans Google Drive (`Processus_AMDEC.xlsx`), réutilise `calculerIPR.ts` tel quel ; premier des 3 chantiers P1 restants, ordre choisi explicitement par l'utilisateur. TD-028 ajoutée le 28/08/2026 (Phase 30) — Compliance Engine généralisé : factorisation du patron règle→blocage déjà répété 3 fois indépendamment (`verifierBlocageExport`/`gardesFinalisation`/`gelerContentPlan`) en un moteur générique pur, refactor comportement-identique (aucune suite de tests existante modifiée) ; portée choisie explicitement par l'utilisateur parmi 3 options pour éviter d'inventer des règles métier sans source réelle.*
